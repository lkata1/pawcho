{
  "name": "server",
  "version": "1.0.0",
  "type": "module",
  "module": "./src/index.ts",
  "private": true,
  "scripts": {
    "build": "bunx tsc --noEmit --project tsconfig.json && bun build.ts",
    "dev": "bun src/index.ts start",
    "preview": "dist/server start",
    "test": "bun src/index.ts test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^6"
  }
}
